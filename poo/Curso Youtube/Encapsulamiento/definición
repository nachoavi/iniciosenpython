La encapsulación:

Es el ocultamineto de datos del estado interno
principalmente pra protger la integridad del objeto



Otras definiciones:

En la Programación orientada a objetos, la encapsulación se refiere a 
la agrupación de datos con los métodos que operan en esos datos, o la restricción del 
acceso directo a algunos de los componentes de un objeto.

